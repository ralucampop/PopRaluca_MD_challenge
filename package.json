{
  "devDependencies": {
    "cypress": "^9.3.1",
    "@shelex/cypress-allure-plugin": "^2.23.0",
    "allure-commandline": "^2.17.2",
    "cypress-xpath": "^1.6.2",
    "fs-extra": "^10.0.0",
    "path": "^0.12.7"
  },
  "scripts": {
    "test-api-and-fe": "npx cypress run --headless --browser chrome --env allure=true",
    "test-api-and-fe-headed": "npx cypress run --headed --browser chrome --env allure=true",
    "report": "npx allure generate --clean && npx allure open"
  }
}
